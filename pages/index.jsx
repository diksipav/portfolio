/** @jsx jsx */
import { jsx } from "theme-ui";

export default () => (
  <div sx={{ height: `calc(100vh - 70px)` }}>
    <div
      sx={{
        variant: "containers.page",
        display: "flex",
        alignItems: "center",
        height: "100%",
      }}
    ></div>
  </div>
);
